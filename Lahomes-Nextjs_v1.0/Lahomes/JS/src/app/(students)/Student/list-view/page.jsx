
import StudentsList from './components/StudentsList';


export const metadata = {
  title: 'Students List'
};

const ListViewPage = () => {
  return <>
 
     < StudentsList/>
 
    </>;
};

export default ListViewPage;